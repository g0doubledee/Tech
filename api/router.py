"""
API Router — All endpoints
TECH Core adapted for API reliability
"""

from fastapi import APIRouter

router = APIRouter()

# Endpoints are defined in main.py — router is available for modular expansion